/* Scorify cursor effect disabled in production build to keep the native pointer visible.
   The file is intentionally left as a no-op so existing templates can keep including it safely. */
(() => {})();
